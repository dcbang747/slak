# Python deps for the Vercel serverless function (api/index.py).
# Mirrors backend/requirements.txt minus uvicorn (Vercel provides the ASGI server).
fastapi==0.115.0
pydantic==2.9.2
python-multipart==0.0.12
